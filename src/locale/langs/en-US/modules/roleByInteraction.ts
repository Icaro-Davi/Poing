import RoleByInteractionType from "../../../baseLocale/modules/roleByInteraction";

const roleByInteraction: RoleByInteractionType = {
    "embed": {
        "title": "Interactive roles",
        "description": "Your roles have been updated successfully."
    }
}

export default roleByInteraction;